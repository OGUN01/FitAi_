/**
 * All onboarding services are defined in the flat file.
 * This barrel re-exports for backward compatibility.
 */
export {
  PersonalInfoService,
  DietPreferencesService,
  BodyAnalysisService,
  WorkoutPreferencesService,
  AdvancedReviewService,
  OnboardingProgressService,
  OnboardingUtils,
} from '../onboardingService';
