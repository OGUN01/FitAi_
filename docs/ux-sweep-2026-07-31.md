# UX Pattern Sweep — 2026-07-31

End-to-end hunt for the 12 anti-patterns. 8 finder agents + 8 adversarial verifiers, 77 surviving findings.

## onboarding (10)

### HIGH P8 Nested interactives — src/components/onboarding/body/PhotoAnalysisSection.tsx:128 [CONFIRMED]
- Evidence: removeBtn Pressable (onPress removePhoto) is rendered inside the photoCard Pressable (onPress openPhotoOptions, line 105) — an interactive ancestor wrapping an interactive child, which emits invalid nested <button> on react-native-web
- Fix: Make the outer tile a plain View with onTouchEnd/onPress only on the image area, or hoist the remove button out of the card Pressable as an absolute-positioned sibling above it (zIndex), same pattern AdjustmentWizard uses for its backdrop.
- Verifier: Real and live: removeBtn Pressable (accessibilityRole="button", line 128-139) is nested inside the photoCard Pressable (accessibilityRole="button", line 105-111). Component renders via BodyAnalysisTab (imported line 27, rendered line 209) which is wired into OnboardingContainer, the live flow from MainNavigation. Emits nested <button> on react-native-web. High severity justified.

### MEDIUM P6 Cramped/small targets — src/components/onboarding/body/PhotoAnalysisSection.tsx:292 [CONFIRMED]
- Evidence: removeBtn style is width: 20, height: 20 with no hitSlop — a 20pt destructive touch target on a photo thumbnail (and `reanalyze` is just padding: xs around a 16px icon)
- Fix: Bump the button to 44pt (or keep 20pt visual with hitSlop {top:12,bottom:12,left:12,right:12}) and give reanalyze the same hitSlop treatment.
- Verifier: removeBtn style is width:20/height:20 (lines 296-297) with no hitSlop on the Pressable (lines 128-133); reanalyze is padding: xs only (lines 355-357). 20pt destructive target on a live screen. Medium is correct.

### MEDIUM P3 Hardcoded locale — src/components/onboarding/review/WeightManagementSection.tsx:150 [CONFIRMED]
- Evidence: `<Text style={styles.heroCurrent}>{currentWeight} kg</Text>` — kg/kg-wk/cm strings are hardcoded across the redesigned Body + Review sections (also GoalVisualizationSection 'Target kg'/'kg/week' lines 222/315, MeasurementsSection ' cm'/' kg' lines 128/166) while personalInfo.units metric/imperial exists on S1 and the rest of the app converts via toDisplayWeight
- Fix: Thread personalInfo.units into the Body/Review sections and format weights/distances with the existing toDisplayWeight/formatWeight util so imperial users see lb/ft-in as the primary readout (kg stays the stored unit).
- Verifier: personalInfo.units ('metric'|'imperial') exists on the onboarding type (src/types/onboarding/personal-info.ts line 40) and the rest of the app gates display via units (useHomeLogic line 65, useProgressScreen line 56) with toDisplayWeight (src/utils/units.ts). WeightManagementSection renders raw '{currentWeight} kg'/'{targetWeight} kg' (lines 150-152) and unit="kg"/"kg/wk" (177-193) with no conversion; GoalVisualizationSection lines 207/222/248/315 same. Partial mitigation: MeasurementsSection already shows a live secondary lb/ft-in conversion (lines 64-76, 115, 153), so primary-unit switching is the remaining gap. Medium correct.

### MEDIUM P1 Duplicate indicators — src/components/onboarding/diet/CookingPreferencesSection.tsx:160 [CONFIRMED]
- Evidence: The header already renders the accent key number `{formData.max_prep_time_minutes ?? 30} min` (lines 146-152) and the RangeSlider below it is used WITHOUT showValue={false}, so its default floating value bubble shows the same '30 min' a second time directly above the thumb — the exact overlap bug GoalVisualizationSection fixed by passing showValue={false}
- Fix: Pass showValue={false} to the RangeSlider (value already lives in the header row), matching PreferencesSection and MeasurementsSection usage.
- Verifier: Header shows '{max_prep_time_minutes ?? 30} min' (lines 146-152) and the RangeSlider at line 160-168 omits showValue, so it defaults to true (RangeSlider.tsx line 73) and renders the floating value bubble with the same '30 min' (lines 159-166). Sibling PreferencesSection passes showValue={false} (line 169). Real duplication, medium correct.

### MEDIUM P1 Duplicate indicators — src/components/onboarding/OnboardingProgressIndicator.tsx:189 [PLAUSIBLE]
- Evidence: Same progress shown four ways stacked: hero stat '{overallCompletion}%', 'Step {currentTab} of {totalTabs}' subtitle, the AuroraStepRail, AND '{completedTabs.length} of {totalTabs} steps completed' — then ProgressStep circles below repeat per-step state the rail already carries
- Fix: Keep the hero % plus the rail (the redesign's single signature indicator) and drop either the ordinal line or the 'N of M completed' line; let the detail list carry per-step errors only.
- Verifier: The four encodings exist as described (hero % line 189-192, 'Step X of Y' line 193-195, AuroraStepRail line 200, 'N of M steps completed' line 201-203, plus per-step ProgressStep rows). But this component only renders inside the dedicated full-screen progress modal (OnboardingContainer line 155-167, gated on showProgressModal) — a detail view whose entire purpose is progress, where summary + visual + per-step detail (with validation errors) is a defensible structure. Downgrade to low: trim at most one of the two redundant text lines.

### MEDIUM P11 Hardcoded user-data fallback — src/components/onboarding/OnboardingModals.tsx:37 [PLAUSIBLE]
- Evidence: userName={personalInfo?.first_name || "Champion"}, goal: (primary_goals)[0] || "Get Fit", workoutsPerWeek: workout_frequency_per_week || 3 — fake name/goal/frequency substituted on the completion modal when real values are missing (violates project principle #8)
- Fix: Surface null/omit the stat when the real value is unavailable (or hide the stat block) and console.warn; never render placeholder user data on the payoff screen.
- Verifier: Fallbacks exist exactly as quoted (lines 37, 40-41) and literally violate principle #8. But first_name is a required field (screenValidation.ts line 77) and the completion modal only shows after all tabs validate, so 'Champion'/'Get Fit'/3 fire only in an already-broken state — real users essentially never see them. Downgrade to low; fix direction (omit stat + warn) still correct.

### LOW P1 Duplicate indicators — src/components/onboarding/WarningCard.tsx:322 [CONFIRMED]
- Evidence: The pace panel's goal summary repeats 'Your goal X kg/week · Target Y kg' and 'Z kg to lose' — the same numbers the WeightManagementSection hero (current → target, 'N.N kg to lose') and Weekly Rate row already show on the same AdvancedReviewTab screen
- Fix: Trim the summary to what the tier ladder doesn't show (e.g. keep only the requested-rate context) or drop it; the hero above already anchors target + delta.
- Verifier: Both render on AdvancedReviewTab (WeightManagementSection line 249, WarningCard line 279). WeightManagementSection already shows current→target hero (lines 150-152) and 'N.N kg to lose/gain' (line 111); WarningCard's goal summary repeats Target Y kg (lines 330-333) and 'Z kg to lose' (lines 335-339). The requested-rate number (lines 325-329) is unique and should stay. Low severity correct — partial duplication, quiet caption styling.

### LOW P12 Duplicate source of truth — src/screens/onboarding/tabs/DietPreferencesTab.tsx:99 [PLAUSIBLE]
- Evidence: useStoredCountry() reads personalInfo.country out of the AsyncStorage auto-save blob with a 700ms setTimeout retry instead of the runtime store — a second read path for data the store owns (principle #6: store is the runtime source), with a debounce race the retry only papers over
- Fix: Pass country through OnboardingContainer props (the same way bodyAnalysisData/personalInfoData reach WorkoutPreferencesTab/BodyAnalysisTab) and delete the AsyncStorage side-channel.
- Verifier: Code is as described, but it is a deliberate, documented workaround (comment lines 63-69: 'the only mid-onboarding cross-tab channel that does not require new props through OnboardingContainer'), read-only, with null fallback and console.warn. The 700ms retry covers the ~200ms auto-save debounce and the tab remounts on switch, so the practical race window is tiny. Low severity correct; the prop-threading fix is the right cleanup, not a bug fix.

### LOW P7 Dead/half-wired input — src/screens/onboarding/tabs/PlanTab.tsx:2 [CONFIRMED]
- Evidence: The entire 7-screen flow (YouTab, GoalTab, BodyTab, FuelTab, TrainingTab, RhythmTab, PlanTab + screenValidation.ts/ONBOARDING_SCREENS) is imported by no container — the live MainNavigation → OnboardingContainer renders the 5-tab flow (PersonalInfo/Diet/Body/Workout/AdvancedReview); ~1,700 lines of dead screens, several still listed as modified
- Fix: Either wire the 7-screen container in or delete the unreferenced screens (MeasurementsSection already calls BodyTab an 'unreferenced experiment'); dead screens drift from hooks/types they duplicate.
- Verifier: Verified: zero imports of YouTab/GoalTab/BodyTab/FuelTab/TrainingTab/RhythmTab/PlanTab anywhere in src (grep for import statements returns nothing); screenValidation.ts is imported only by the dead PlanTab; the live path is MainNavigation -> OnboardingContainer (MainNavigation.tsx lines 18, 647) rendering the 5-tab flow. ~1,700 lines of dead screens that drift from shared hooks/types. Low severity correct — dead code, not a runtime bug.

### LOW P7 Dead/half-wired input — src/components/onboarding/RateComparisonCard.tsx:38 [CONFIRMED]
- Evidence: RateComparisonCard has zero importers — WarningCard absorbed the pace-picker role ('SmartAlternatives data (previously in RateComparisonCard)') yet the 318-line component was still modified in the redesign
- Fix: Delete the file (and its test references if any) so the pace-picker has one implementation — WarningCard's volt pace panel.
- Verifier: Only two references exist: a comment in WarningCard.tsx line 53 ('SmartAlternatives data (previously in RateComparisonCard)') and a test import (AdvancedTouchTargets.test.tsx line 149, rendered line 221). No production importer — WarningCard's pace panel absorbed the role. 318-line dead component; delete along with its test block. Low severity correct.

## home-nav (7)

### MEDIUM P4 Coupled/stale controls — src/screens/main/home/HomeHeader.tsx:56 [CONFIRMED]
- Evidence: const hour = new Date().getHours(); feeds useMemo(..., []) with eslint-disable — greeting/icon/gradient frozen at mount time
- Fix: Compute greeting from a state value refreshed on an interval (or on AppState 'active'), or drop useMemo and derive per render so the greeting tracks the actual time of day
- Verifier: Real: useMemo([]) freezes greeting/icon/gradient at mount; tab screens stay mounted so a session crossing noon keeps 'Good morning'. Cosmetic only — P4 fit is loose (stale derived display, no coupled control). Medium is borderline; low would also be defensible.

### MEDIUM P7 Dead/half-wired input — src/components/home/QuickActionsConfig.tsx:26 [CONFIRMED]
- Evidence: createQuickActions destructures only onLogWeight/onLogMeal/onLogWater/onBarcodeScan/onScanLabel — isHealthKitAuthorized, isHealthConnectAuthorized, syncHealthData, syncFromHealthConnect, onScanFood, onRecipes are accepted but silently dropped; HomeScreen.tsx:151-185 builds web-alert sync wrappers that go nowhere
- Fix: Remove the dead props from the interface and the dead wiring in HomeScreen (including the wearableConnected aliasing at lines 112-113), or actually render sync/scan-food/recipes actions
- Verifier: Verified: 6 of 11 props silently dropped; HomeScreen:156-175 builds web-alert sync wrappers that can never fire. Downgrade to low — dead wiring/maintenance smell, every rendered chip works, nothing user-facing breaks.

### MEDIUM P7 Dead/half-wired input — src/components/home/HealthIntelligencePlaceholder.tsx:71 [CONFIRMED]
- Evidence: Card invites 'Connect Health Data / Sync Apple Health or Health Connect' but onPress is HomeScreen's handleHealthHubPress → onNavigateToTab('analytics') — tapping a connect CTA lands on the Analytics tab with no connect affordance
- Fix: Route the placeholder's onPress to the wearable connection/settings flow (or trigger initializeHealthKit/initializeHealthConnect), not the analytics tab
- Verifier: Verified: placeholder (rendered when no health data) invites 'Connect Health Data' with a chevron, but onPress routes to the analytics tab (HomeScreen:121), and grep confirms AnalyticsScreen has zero connect/sync affordances. Misleading CTA; medium is right.

### LOW P1 Duplicate indicators — src/screens/main/home/HealthIntelligenceHub.tsx:110 [CONFIRMED]
- Evidence: TodayHero renders heroInsightText (from the same useHealthIntelligenceLogic recovery insight) with a recovery-colored dot, and one card below the hub shows the same recoveryScore as a giant number plus the same recoveryLabel/status badge — same recovery info presented twice adjacently
- Fix: Use a non-recovery coach line in TodayHero when the Health Intelligence card is visible, or drop the status badge in the hub since the hero already conveys the recovery state
- Verifier: Verified: with real data the TodayHero coach line IS the hook's recovery sentence with recovery-colored dot (HomeScreen:240), and the hub below shows the same recoveryScore as a giant number plus the same recoveryLabel badge. Same recovery state twice adjacently; low fits.

### LOW P1 Duplicate indicators — src/screens/main/HomeScreen.tsx:239 [CONFIRMED]
- Evidence: Header shows 'Good morning, {userName}' and the very next element (TodayHero coach line) starts with `${first}, your personalized plan...` — the user's first name is repeated in two stacked elements
- Fix: Drop the name prefix from the hero fallback lines; the header already owns the personalized greeting
- Verifier: Verified: no-wearable users see 'Good morning, {userName}' in the header immediately followed by a hero line starting with the same first name. Real duplication in stacked elements; low fits.

### LOW P7 Dead/half-wired input — src/screens/main/home/DailyProgressRings.tsx:172 [CONFIRMED]
- Evidence: stepsSource: _stepsSource destructured and never referenced — HomeScreen passes it with the comment 'Hide stale source attribution with stale metrics' but no attribution is ever rendered
- Fix: Either render the source attribution (e.g. small 'via Health Connect' caption on the Steps chip) or remove the prop and its HomeScreen wiring
- Verifier: Verified: stepsSource destructured as _stepsSource and never referenced; HomeScreen:361 passes it with a comment about attribution that was never implemented. Dead prop; low fits.

### LOW P7 Dead/half-wired input — src/screens/main/home/index.ts:11 [CONFIRMED]
- Evidence: MotivationBanner, TodaysFocus, SmartCoaching, HydrationTracker, and home/AchievementShowcase are exported but rendered nowhere (SmartCoaching has zero references; HomeScreen comment says TodaysFocus was dropped)
- Fix: Delete the dead components and their exports, or re-integrate them into the Home layout
- Verifier: Verified via grep: MotivationBanner, TodaysFocus, SmartCoaching, HydrationTracker, and home/AchievementShowcase render nowhere (only test stubs). The analytics AchievementShowcase used by AnalyticsScreen:792 is a separate live component — finding correctly scopes to the home one. Low fits.

## diet (9)

### HIGH P7 Dead/half-wired input — src/components/diet/CreateRecipeModal.tsx:31 [CONFIRMED]
- Evidence: const geminiService = { isAvailable: () => false, ... } — handleCreateRecipe always hits 'Feature Not Available' (line 136)
- Fix: The entire Create Recipe modal (4 prompt fields, suggestion chips, progress bar, create button) is wired to a stub that can never succeed, yet is reachable from DietActionDock 'Recipes'. Either wire it to the Cloudflare Workers recipe endpoint or remove the Recipes action/modal until the backend exists; do not let users fill a form that always fails.
- Verifier: Verified: geminiService stub returns isAvailable()===false (line 32), and handleCreateRecipe (line 136) always alerts 'Feature Not Available'. Reachable: DietScreen line 1110 onRecipes -> setShowCreateRecipe(true) -> DietModals line 114 renders the modal. Users fill 4 prompt fields and hit a guaranteed dead end. High severity is appropriate (user explicitly complained about this class of half-wired feature).

### HIGH P7 Dead/half-wired input — src/components/diet/PortionAdjustment.tsx:64 [CONFIRMED]
- Evidence: onTouchEnd={handleTrackPress} on the slider container; const isDragging = false is a dead constant (line 45) with a style that never applies
- Fix: The custom 'slider' has no drag gesture at all — it only jumps to wherever the finger lifts, and the thumb-active state is unreachable. Replace with a real PanResponder/GestureDetector drag (or the shared RangeSlider) so the thumb tracks the finger; delete the dead isDragging code.
- Verifier: Verified: only onTouchEnd={handleTrackPress} on the container (line 64) — no PanResponder/GestureDetector, so the thumb never tracks the finger; value jumps to the lift point. isDragging=false (line 45) is a dead constant; customSliderThumbActive style is unreachable. Severity adjusted high -> medium: the control still sets a value on tap/release, and quick-portion buttons + gram input offer working alternatives, so it is degraded UX rather than a broken flow.

### MEDIUM P4 Coupled/stale controls — src/components/diet/PortionAdjustment.tsx:382 [CONFIRMED]
- Evidence: value={String(currentAdjustment.adjustedGrams)} with onChangeText that ignores NaN, so clearing the field snaps back to the old value; input allows 1–2000g while the slider floor is max(20, 0.3×estimate)
- Fix: Two coupled controls for the same value with mismatched constraints and an input the user cannot clear/retype (empty string is rejected, old value re-renders). Keep a local text state while editing, commit on valid parse/onBlur, and clamp both controls to the same min/max.
- Verifier: Verified: controlled TextInput value={String(adjustedGrams)} with onChangeText that drops NaN (line 384-387) — clearing the field snaps back to the old value, so users cannot retype from scratch. Input accepts 1–2000 while the slider clamps to max(20, 0.3x estimate)..3x estimate (lines 257-258), so the two controls for one value have mismatched ranges. Medium severity is fair.

### MEDIUM P1 Duplicate indicators — src/components/diet/MealsTimeline.tsx:172 [CONFIRMED]
- Evidence: Each row encodes status three ways: colored timeline dot (line 117), StatusPill (line 172), and a colored progress micro-bar (line 178) — all driven by the same MealPlanStatus
- Fix: Pick one primary status encoding (the StatusPill text) and one ambient one; the dot color, pill color, and bar color all restate the same state in a single 3-line row. Keep the timeline dot neutral/structural and let the pill carry status.
- Verifier: Verified: status is color-encoded three times in one row — timelineDot dotStyle (lines 95-102, 117), StatusPill with text (line 172), and progressBar backgroundColor: progressColorFor(status) (line 179). Severity adjusted medium -> low: the micro-bar's width encodes progress % (distinct datum) and the dot is timeline structural; only the redundant third color signal is the issue.

### LOW P1 Duplicate indicators — src/components/diet/MealDetailView.tsx:259 [CONFIRMED]
- Evidence: 'Step {currentStep + 1} of {total}' counter + stepDots strip + numbered stepBadge inside the card all show the same position
- Fix: The StepViewer stacks counter text, a dot strip, and a numbered badge for one datum. Drop the dot strip (or the counter) — dots add nothing past ~5 steps where they stop being scannable anyway.
- Verifier: Verified: StepViewer stacks 'Step {currentStep+1} of {total}' counter (line 259-261), a stepDots strip (line 262-269), and a numbered stepBadge inside the card (line 273-275) — three encodings of one position datum. Low severity is correct.

### LOW P1 Duplicate indicators — src/components/diet/MealDetailView.tsx:514 [CONFIRMED]
- Evidence: headerTitle renders {mealLabel} and the typeRow directly below the hero renders the same {mealLabel} again (line 626)
- Fix: Meal type ('Lunch') appears as the screen header and immediately again as a labeled row with icon. Make the typeRow carry only new info (status pill already there) or change the header title to the meal name.
- Verifier: Verified: headerTitle renders {mealLabel} (line 514) and the typeRow immediately below the hero renders the same {mealLabel} again with an icon (line 626). The row does also carry the StatusPill, so only the label itself is duplicated — low severity is right.

### LOW P1 Duplicate indicators — src/components/diet/ConcentricRings.tsx:294 [CONFIRMED]
- Evidence: Protein/carbs progress is drawn as rings, then the legend re-states both as numeric 'x/y g' plus a 2px mini progress bar — three encodings of the same two values
- Fix: The legend's mini progress bars duplicate what the rings already show; keep the numeric legend (rings can't show grams) but remove the redundant 2px bars, or drop the rings' claim and keep one viz per metric.
- Verifier: Verified: LegendItem (lines 339-364) renders numeric 'x/y g' plus a 2px legendBarFill progress bar for Protein and Carbs — values already drawn as rings. The numeric legend adds info rings cannot show, but the mini bars are pure duplication of ring fill. Low severity is correct.

### LOW P6 Cramped/small targets — src/components/diet/WeekCalendarStrip.tsx:122 [CONFIRMED]
- Evidence: dayPill: { width: rw(44), height: rh(68) } with no minWidth — rw scales down to ~36pt on a 320pt-wide device
- Fix: Day-pill touch targets shrink below 44pt on small phones because width is purely rw-scaled. Add minWidth: 44 (like the Math.max(rw(44), 44) pattern used elsewhere) or hitSlop.
- Verifier: Verified: dayPill width is purely rw(44) with no minWidth/hitSlop (lines 121-131); rw scales with device width so on a 320pt-class phone the target drops to ~37-38pt, below the 44pt minimum. Edge case on small devices only — low severity is right.

### LOW P6 Cramped/small targets — src/components/diet/LogMealModal.tsx:936 [CONFIRMED]
- Evidence: addIngredientBtn style (line 1371) is a bare row with no minHeight/hitSlop — touch target is a 20px icon + 13px text; saveMealLink (line 1585) is only paddingVertical: rh(8) ≈ 30px tall
- Fix: The 'Add' ingredient button and '+ Save this meal for reuse' link are sub-44pt targets in a dense form. Add minHeight: 44 or hitSlop to both, matching the removeIngredient hitSlop={12} pattern already in the file.
- Verifier: Verified both targets: addIngredientBtn style (lines 1371-1375) is a bare row — touch target is a 20px icon + 13px text with no padding/minHeight/hitSlop; saveMealLink (lines 1585-1592) is only paddingVertical rh(8) around a 15px icon + 13px text, ~32px total. Both sub-44pt in a dense form. Low severity is correct.

## workout (9)

### HIGH P8 Nested interactives — src/screens/workouts/TemplateLibraryScreen.tsx:1470 [CONFIRMED]
- Evidence: ListRow's outer AnimatedPressable (accessibilityRole="button") wraps the bookmark button (1499), kebab menu button (1540), up to 3 exercise-history pressables (1580) and the entire inline Edit/Duplicate/Delete menu (1615-1646) — all nested button-role pressables.
- Fix: Make the row body a plain View with onPress handled by the inner content areas only, or move bookmark/kebab/history/menu pressables out as siblings of the row pressable (absolute-positioned overlays), so no Pressable is an ancestor of another.
- Verifier: Real nesting: outer row AnimatedPressable (role=button) wraps bookmark (1499), kebab (1540), history rows (1580), menu items (1615-1646) and StartCta. Severity high -> medium: react-native-web renders Pressable as <div role="button">, not <button>, so the literal button-in-button DOM crash does not occur, and bookmark uses stopPropagation so native behavior works. Real a11y-tree violation and event-routing smell, not a crash.

### HIGH P8 Nested interactives — src/screens/workouts/TemplateLibraryScreen.tsx:1301 [CONFIRMED]
- Evidence: GridCard's outer card AnimatedPressable (accessibilityRole="button", line 1301) contains the bookmark AnimatedPressable (1340) and the StartCta AnimatedPressable (1229/1382) — button-in-button breaks web rendering ("<button> cannot be a descendant of <button>") and the a11y tree.
- Fix: Restructure like MyWorkoutsCard does (its comment documents exactly this rule): keep card pressable and action buttons as siblings — e.g. wrap tile in a non-pressable View, put onPress on the thumbnail/body sub-areas, keep bookmark + Start as independent pressables.
- Verifier: Real nesting: GridCard outer pressable (1301) contains bookmark pressable (1340) and StartCta pressable (1229 via 1382). Same severity correction as the ListRow finding: medium, not high — no DOM crash under react-native-web (div+role), functional on native; a11y/structure violation only.

### HIGH P3 Hardcoded locale — src/components/fitness/builder/SetRow.tsx:351 [CONFIRMED]
- Evidence: Weight input is hardcoded `label="kg"` (and again at 411 for drop-set weight) — the whole builder stack (SetRow, ExerciseEditorSheet:805, BuilderSummaryFooter:251, MuscleHeatmap:85) never reads profileStore personalInfo.units, while WorkoutSessionScreen:167 derives userUnits and converts. An imperial user types pounds into a field labeled kg and the value is persisted as kilograms into the plan.
- Fix: Read personalInfo.units via profileStore in ExerciseEditorSheet/SetRow, label the input with the user's unit, and convert on commit/display using the existing src/utils/units.ts toDisplayWeight/convertWeight helpers (store kg internally, display lbs).
- Verifier: Verified: label="kg" at 351 and 411, ExerciseEditorSheet hardcodes kg at 778/805, BuilderSummaryFooter:251 and MuscleHeatmap:85 likewise; src/utils/units.ts exists and WorkoutSessionScreen:167 already derives userUnits. Imperial user input is persisted under a kg label — data-integrity issue, high severity is correct.

### HIGH P7 Dead/half-wired input — src/screens/workouts/TemplateLibraryScreen.tsx:501 [CONFIRMED]
- Evidence: handleUseInSchedule navigates `navigation.navigate("WeeklyBuilder", { sourceTemplateId: template.id })` with the comment "the template is intentionally unused here" — a repo-wide grep shows sourceTemplateId has zero consumers, so the TemplateDetailSheet 'Use in Schedule' button silently drops the template and lands the user on an empty builder.
- Fix: Either implement the Phase-9 hydration (WeeklyBuilderScreen reads route.params.sourceTemplateId → buildDayWorkoutFromTemplate + workoutBuilderStore.hydrateFromPlan) or remove/relabel the button until it works.
- Verifier: Verified: repo-wide grep shows sourceTemplateId has exactly one occurrence (the navigate call) and zero consumers. The 'Use in Schedule' button silently drops the template. Severity high -> medium: navigation itself works and the TODO documents deliberate Phase-9 deferral; misleading affordance, not broken/crash/data-loss.

### MEDIUM P7 Dead/half-wired input — src/screens/workouts/CreateWorkoutScreen.tsx:366 [CONFIRMED]
- Evidence: In edit mode the difficulty/category/tags inputs (lines 802-896) stay editable, but the edit save path calls updateTemplate with only {name, exercises, targetMuscleGroups, estimatedDurationMinutes, isPublic} — shareCategory/shareDifficulty/shareTagsInput are silently discarded on save (comment admits 'community metadata edits … out of scope').
- Fix: Extend workoutTemplateService UpdateInput + update query to accept category/difficulty/tags (additive), or disable those inputs in edit mode with an explanatory note so the UI stops promising edits it won't persist.
- Verifier: Verified: share difficulty/category/tags inputs (802-896) remain editable in edit mode with no isEditing disable, while updateTemplate (366-372) only sends name/exercises/muscleGroups/duration/isPublic — edits silently discarded. Medium is right.

### MEDIUM P3 Hardcoded locale — src/screens/workouts/ExerciseHistoryScreen.tsx:83 [CONFIRMED]
- Evidence: formatSets returns `${sets.length}×${reps[0]} @ ${weight}kg`; the chart title is hardcoded 'Volume (kg)' (line 135) and PRs render `" kg"` / `" kg (1RM)"` (line 244) — the screen never reads profileStore units, so imperial users see kg throughout their history/PRs while the session screen shows lbs.
- Fix: Select personalInfo.units from profileStore and run values through utils/units.ts toDisplayWeight, rendering 'kg'/'lbs' labels accordingly.
- Verifier: Verified: kg hardcoded at 83, 85, 135 ('Volume (kg)'), 244 (' kg (1RM)'), and 409; file never imports profileStore or units utils while the session screen converts for imperial users. Medium is right.

### MEDIUM P3 Hardcoded locale — src/components/workout/WorkoutHeader.tsx:105 [CONFIRMED]
- Evidence: Meta line always renders `${Math.round(sessionVolume).toLocaleString()} KG` while the same screen's warm-up/set weights are shown in lbs for imperial users (WorkoutSessionScreen:817-819) — one screen mixing two unit systems.
- Fix: Pass userUnits (already computed in WorkoutSessionScreen:167) into WorkoutHeader and convert sessionVolume via utils/units.ts before rendering the unit suffix.
- Verifier: Verified: line 105 renders `${Math.round(sessionVolume).toLocaleString()} KG` unconditionally; WorkoutSessionScreen:167 computes userUnits and passes it to other children (line 978) but not to WorkoutHeader (708-722). One screen mixes units for imperial users. Medium is right.

### MEDIUM P1 Duplicate indicators — src/components/fitness/builder/WeeklyInsightsPanel.tsx:287 [CONFIRMED]
- Evidence: Insights panel renders a 'Volume' StatTile (`String(Math.round(insights.totalVolume))` + ' kg') while the sticky BuilderSummaryFooter on the same WeeklyBuilderScreen renders the identical 'Volume' stat (`${Math.round(totalVolume)}kg`, BuilderSummaryFooter.tsx:251) — same metric shown twice on one screen.
- Fix: Drop the Volume tile from the insights grid (footer already carries it, sticky and always visible) or drop it from the footer; keep one authoritative volume readout per screen.
- Verifier: Verified: insights Volume tile (287-296, value Math.round(insights.totalVolume) + ' kg') duplicates the sticky footer Volume stat (BuilderSummaryFooter:251, `${Math.round(totalVolume)}kg`) — both mounted on WeeklyBuilderScreen (512 panel, 520 footer), footer always visible. Borderline low/medium; keep medium since the footer is sticky so the duplication is persistent on screen.

### LOW P1 Duplicate indicators — src/screens/workout/WorkoutSessionScreen.tsx:871 [CONFIRMED]
- Evidence: Set indicator stacks three representations of the same number: giant numeral `{completedSetsCount + 1} / {totalSets}` (871-880), a set-dot row (881-888), and the footer CTA repeating `Continue — Set ${completedSetsCount + 1} of ${totalSets}` (904).
- Fix: Keep the giant numeral + dots and simplify the CTA to 'Continue' (or 'Start Set N' only), so the current-set info is stated once.
- Verifier: Verified: giant numeral N / total (870-880), set-dot row (881-888), and footer CTA 'Continue — Set N of M' (904) state the same current-set info three ways, all simultaneously visible in preview phase. Low severity polish call is accurate.

## progress (11)

### HIGH P7 Dead/half-wired input — src/screens/main/analytics/GoalProgressCard.tsx:33 [CONFIRMED]
- Evidence: getWeightGoalProgress called with no weightHistory and fallbackStartWeightKg = calculatedMetrics?.currentWeightKg (same as currentWeightKg), so startWeightKg === currentWeightKg → progressToDate = 0 → weightProgress is always 0
- Fix: Pass weightHistory from analyticsStore (like GoalProgressSection does) or a real onboarding start weight as fallbackStartWeightKg, so progressToDate/totalToTarget is meaningful; hide the bar when no start weight exists instead of rendering a permanent 0%.
- Verifier: Verified against goalProgressUtils.ts:45-49 — whenever currentWeightKg is non-null (required for render), fallbackStartWeightKg equals it, so progressToDate is always 0 and the bar permanently shows 0%. High severity is justified: broken indicator visible to every user with a weight goal.

### MEDIUM P7 Dead/half-wired input — src/components/progress/GoalProgressSection.tsx:126 [CONFIRMED]
- Evidence: weeklyProgress.caloriesConsumed is read here, but useProgressScreen.ts (lines 210-214) only provides { workoutsCompleted, mealsCompleted, streak } — the 'Calorie Adherence' GoalRow can never render
- Fix: Either compute weekly caloriesConsumed in useProgressScreen (from nutritionStore, as HomeScreen/useHomeLogic does) and add it to weeklyProgress, or delete the dead calorie GoalRow and its doc comment.
- Verifier: useProgressScreen.ts:210-214 provides only workoutsCompleted/mealsCompleted/streak, so the Calorie Adherence GoalRow can never render. Severity should be LOW not medium — invisible dead code, no user-facing breakage; it is a silently missing documented feature, not a visible defect.

### MEDIUM P12 Duplicate source of truth — src/components/progress/WeightJourneySection.tsx:149 [CONFIRMED]
- Evidence: currentWeightKg = rawChartData[rawChartData.length-1] where rawChartData is filtered by the selected period cutoff — with period '1W'/'1M' and no recent entries the hero shows '—' and 'Log at least 2 entries' even when older weightHistory exists
- Fix: Derive the hero current weight from the unfiltered latest weightHistory entry (SSOT), and only use the filtered array for the chart; change the empty-chart copy to 'No entries in this period' when unfiltered data exists.
- Verifier: rawChartData is period-filtered (line 132 cutoff) before currentWeightKg reads its last entry, so 1W/1M with no recent entries renders hero '—' and the 'Log at least 2 entries' empty state despite older weightHistory existing. Medium is fair.

### MEDIUM P1 Duplicate indicators — src/screens/main/ProgressScreen.tsx:119 [CONFIRMED]
- Evidence: WeightJourneySection hero shows current weight + 'Goal X · Y kg to lose' (lines 246-275) and the immediately following GoalProgressSection repeats 'Weight Goal: X kg now / Goal Y kg' with a progress bar — same weight/goal info stacked twice
- Fix: Keep the hero current/delta in WeightJourneySection and drop the weight GoalRow from GoalProgressSection (or remove the goalHint line), so weight-to-goal appears exactly once.
- Verifier: WeightJourneySection goalHint (lines 270-275) and GoalProgressSection weight GoalRow (lines 161-171) do repeat current/target weight adjacently, but the GoalRow adds %-progress and a bar the hero lacks. Severity should be LOW — mild overlap, not stacked identical indicators.

### MEDIUM P3 Hardcoded locale — src/screens/main/analytics/MetricSummaryGrid.tsx:247 [CONFIRMED]
- Evidence: trendValue hardcodes `${data.weight.change.toFixed(1)} kg` and the Weight stat value is raw calculatedMetrics.currentWeightKg with no toDisplayWeight conversion — imperial users see kg everywhere on the Analytics tab
- Fix: Accept a unit prop (as ProgressTrendsScreen does with weightUnit + toDisplayWeight) and convert value/change before formatting, suffixing the resolved unit.
- Verifier: formatWeight (lines 187-191) does no conversion, trendValue hardcodes ' kg', and AnalyticsScreen passes raw kg with no toDisplayWeight anywhere. Imperial users see kg on the whole Analytics tab. Medium is fair.

### MEDIUM P3 Hardcoded locale — src/screens/main/analytics/TrendCharts.tsx:75 [CONFIRMED]
- Evidence: <LineChart data={weightData || []} color={chart[1]} unit="kg" /> — chart axis/tooltip/trend copy ('Gained X kg over this period') hardcoded to kg while AnalyticsScreen feeds raw kg values
- Fix: Thread weightUnit from profileStore (personalInfo.units) through AnalyticsScreen → TrendCharts and convert the data points once, mirroring displayWeightTrend in ProgressTrendsScreen.
- Verifier: unit="kg" literal confirmed; AnalyticsScreen:421-429 feeds raw weightHistory kg values unconverted. Medium is fair.

### MEDIUM P12 Duplicate source of truth — src/components/charts/index.ts:4 [CONFIRMED]
- Evidence: Barrel comment admits ColorCodedZones, WeightProjectionChart and AnimatedChart exist BOTH in charts/ and ui/ ('NOT re-exported here to avoid an ambiguous-duplicate export'); the charts/ copies (plus ProgressChart, WorkoutIntensityChart) have zero production imports — two drifting implementations
- Fix: Delete the unused charts/ copies (ProgressChart, NutritionChart's unused exports, ColorCodedZones, WeightProjectionChart, WorkoutIntensityChart, AnimatedChart) and keep the components/ui versions as the single implementation.
- Verifier: Duplicate implementations are real (barrel comment admits it), but evidence is partially wrong: charts/AnimatedChart IS used in production (ExerciseEditorSheet.tsx:60), as are charts/NutritionChart (MealDetail.tsx:16) and MuscleBalanceRadar — the suggested fix would break production. Genuinely unused: charts/ProgressChart, charts/WorkoutIntensityChart, charts/ColorCodedZones, charts/WeightProjectionChart, plus the ui/ copies of ColorCodedZones/WeightProjectionChart/AnimatedChart (no production imports by name). Severity should be LOW — maintenance/drift risk, not user-visible.

### LOW P12 Duplicate source of truth — src/components/progress/GoalProgressSection.tsx:121 [CONFIRMED]
- Evidence: currentWeightKg comes from progressStats.weightChange.current (progressData service) while the adjacent WeightJourneySection uses the last weightHistory entry (analyticsData service) — two sources for 'current weight' on one screen that can drift
- Fix: Pick one source (analyticsStore weightHistory latest, the same source AnalyticsScreen uses via resolveCurrentWeight) and pass it to both sections from useProgressScreen.
- Verifier: Verified: current weight here is progressStats.weightChange.current (progressData service) while adjacent WeightJourneySection uses the last weightHistory entry (analyticsData service). Two sources on one screen; low severity appropriate.

### LOW P3 Hardcoded locale — src/screens/main/analytics/GoalProgressCard.tsx:94 [CONFIRMED]
- Evidence: goalText renders `${currentWeightKg.toFixed(1)} kg → ${targetWeightKg.toFixed(1)} kg` with hardcoded kg literals, ignoring the units setting
- Fix: Use toDisplayWeight(value, unit) with the profileStore unit, as the parent ProgressTrendsScreen already computes weightUnit.
- Verifier: Hardcoded 'kg' literals in goalText confirmed; parent ProgressTrendsScreen computes weightUnit (line 48-49) but never passes it to GoalProgressCard. Low is appropriate (subset of the same Analytics-tab kg problem as findings 6/7 but distinct component).

### LOW P7 Dead/half-wired input — src/components/progress/ProgressAnalytics.tsx:1 [CONFIRMED]
- Evidence: ProgressAnalytics, ProgressInsights, SummaryStatsSection, TodaysProgressCard, WeeklyChartSection, RecentActivityFeed, RecentActivitiesSection, BodyMetricsSection, WearableActivityCard and the whole components/progress/analytics/ directory have no production imports (verified via grep — only test files reference two of them)
- Fix: Delete the dead components or re-wire the intended ones; keeping ~10 orphaned files with their own data interfaces invites stale-code reuse (as already happened with GoalProgressSection's caloriesConsumed).
- Verifier: Grep across src/ confirms zero production imports for all listed components (ProgressAnalytics, ProgressInsights, SummaryStatsSection, TodaysProgressCard, WeeklyChartSection, RecentActivityFeed, RecentActivitiesSection, BodyMetricsSection, WearableActivityCard, components/progress/analytics/*); only test files reference two. Low severity appropriate.

### LOW P7 Dead/half-wired input — src/screens/main/ProgressScreen.tsx:99 [CONFIRMED]
- Evidence: ProgressHeader requires showAnalytics/setShowAnalytics props and is passed setShowAnalytics={() => {}} — the component body never destructures or uses either prop
- Fix: Remove the unused showAnalytics/setShowAnalytics from ProgressHeaderProps and the no-op arrow at the call site.
- Verifier: ProgressHeaderProps declares showAnalytics/setShowAnalytics (ProgressHeader.tsx:20-21) but the component destructures only navigation/trackBStatus/onAddEntry/onShare (lines 26-31); call site passes a no-op arrow. Dead props; low is right.

## profile-paywall (12)

### HIGH P7 Dead/half-wired input — src/hooks/usePrivacySecurityLogic.ts:54 [CONFIRMED]
- Evidence: toggleSetting flips state saved only to AsyncStorage '@fitai_privacy_settings'; grep confirms zero consumers — Biometric Auth and Auto-Lock toggles enforce nothing
- Fix: Wire biometricAuth/autoLock into an actual app-lock gate (or remove the Security section until implemented); at minimum mark non-functional toggles as 'coming soon' disabled rows instead of letting users believe protection is on.
- Verifier: Grep confirms biometricAuth/autoLock are read only by PrivacySecurityScreen's own toggle UI; persisted to AsyncStorage with zero enforcement consumers. A security toggle that does nothing is worse than a normal dead control (user believes protection is on) — high severity is justified.

### HIGH P7 Dead/half-wired input — src/hooks/useAboutFitAILogic.ts:131 [CONFIRMED]
- Evidence: Share handler ends in crossPlatformAlert('Share', 'Native sharing will be implemented here.'); Rate-the-App ends in 'App Store link will be available after app publication.'
- Fix: Use React Native Share API for Share with Friends (Share.share already used elsewhere) and expo-store-review / StoreReview.requestReview() for Rate the App; hide the rows until store URLs exist.
- Verifier: Verified lines 101-104 and 119/131: both Share and Rate-the-App terminate in 'will be implemented'/'available after publication' alerts. Share.share is already imported and used in usePrivacySecurityLogic.ts and useProfileLogic.ts, so the fix is trivial. Medium-high at most (no crash/data loss) but both primary CTAs on the About screen are placeholders.

### MEDIUM P7 Dead/half-wired input — src/hooks/useHelpSupport.ts:95 [CONFIRMED]
- Evidence: 'Live Chat' button alerts 'Live chat support will be available in the next update!'; handleSystemStatus fallback fabricates 'All FitAI systems are currently operational' with green checkmarks when the URL fails
- Fix: Remove Live Chat from the contact alert until it exists; on status-page failure say 'could not load status' instead of claiming all systems operational.
- Verifier: Both halves verified: Live Chat button alerts 'available in the next update!' (lines 91-97), and the handleSystemStatus catch fallback (lines 228-239) claims 'All FitAI systems are currently operational' with green checkmarks precisely when the status page failed to load — fabricated status info. Medium is right.

### MEDIUM P12 Duplicate source of truth — src/hooks/useProfileLogic.ts:78 [PLAUSIBLE]
- Evidence: Units live in 3 places — AsyncStorage @fitai_units_preference, local unitsPreference state, profileStore.personalInfo.units; the sync effect only pushes store→AsyncStorage, so an AsyncStorage-only value never reaches the store and BodyMeasurementsEditModal (reads personalInfo.units) can disagree with the Settings subtitle
- Fix: Make profileStore.personalInfo.units the single source; on mount migrate any AsyncStorage value into the store once, then delete the AsyncStorage key and local state.
- Verifier: Three copies exist, but the sync effect (lines 92-105) pushes profileStore→local+AsyncStorage and handleUnitsSelect writes all three atomically, so they cannot drift in normal flow. Only a legacy AsyncStorage-only value (store units unset → effect early-returns at line 94) never reaches the store. Real but narrow edge — downgrade to low.

### MEDIUM P3 Hardcoded locale — src/screens/main/profile/modals/BodyMeasurementsEditModal.tsx:398 [CONFIRMED]
- Evidence: Height input hardcodes suffix='cm' and hint='Height in centimeters' while weight converts via weightUnit — an imperial (lbs) user is still forced to enter height in cm
- Fix: Convert height to ft/in (or inches) when personalInfo.units === 'imperial' using the same convertWeight/toDisplayWeight pattern from src/utils/units.
- Verifier: Verified: line 407 suffix="cm" and line 409 hint 'Height in centimeters' are hardcoded while the weight inputs at lines 422/437 use suffix={weightUnit}. Imperial users are forced to enter height in cm. Medium severity stands.

### MEDIUM P3 Hardcoded locale — src/components/subscription/PaywallModal.tsx:146 [CONFIRMED]
- Evidence: getPaywallPrimaryLabel builds 'INR 999/mo' and the modal patches it with .replace('INR ', '₹'); PlanCard renders `₹${pricePerMonth * 12}` with no locale grouping (₹4999 not ₹4,999)
- Fix: Format prices with Intl.NumberFormat('en-IN', {style:'currency', currency:'INR'}) in one shared formatter and pass the finished label down; delete the string-replace hack.
- Verifier: All three parts verified: subscriptionUi.ts:60-61 builds 'Subscribe - INR X/mo', line 146 patches with .replace('INR ', '₹'), and PlanCard.tsx:76 renders ₹{pricePerMonth * 12} with no thousands grouping (₹4999). Medium stands.

### MEDIUM P7 Dead/half-wired input — src/hooks/useProfileLogic.ts:346 [CONFIRMED]
- Evidence: ProfileStats pills are Pressable buttons whose only action is an alert repeating the identical number ('You're on a 3 day streak!' under a pill already showing '3')
- Fix: Navigate to the relevant screen (Progress/Analytics/Achievements) on stat press, or remove the pressable + button role and render as static info.
- Verifier: Verified handleStatPress (lines 346-397): every branch only fires an alert restating the number already rendered on the pill. Dead-ish interaction; navigation or static rendering is the right fix. Medium stands.

### MEDIUM P9 No visual hierarchy — src/hooks/useProfileLogic.ts:518 [CONFIRMED]
- Evidence: 'Rest Timer' row renders with a chevron like every navigation row, but tapping it silently toggles useFitnessStore.setRestTimerEnabled — no switch, no confirmation, affordance identical to rows that navigate
- Fix: Render a Switch (or subtitle-only state with showChevron:false and a toggle control) for boolean settings; keep chevrons only for rows that open screens.
- Verifier: Verified: rest-timer row (lines 518-523) omits showChevron:false (contrast with theme/language rows at 498/515 which correctly set it) yet handleSettingItemPress case 'rest-timer' silently flips the store with no confirmation. Subtitle does show On/Off state, softening it slightly, but the chevron affordance is wrong. Medium stands.

### LOW P1 Duplicate indicators — src/components/subscription/PaywallModal.tsx:396 [CONFIRMED]
- Evidence: termsText 'Subscription auto-renews. Cancel anytime… Secure payments via Razorpay.' duplicates the TrustRow directly above it ('Secure · Razorpay', 'Cancel anytime', 'Auto-renews')
- Fix: Keep the TrustRow chips and shrink the footer to only the legal/auto-renew disclosure required by app-store rules, removing the redundant phrases.
- Verifier: Verified: TrustRow.tsx:28-32 shows 'Secure · Razorpay', 'Cancel anytime', 'Auto-renews' and termsText repeats all three verbatim in sentence form. Genuine duplication; low severity is correct since the auto-renew sentence may be legally required — the fix (trim to legal disclosure only) is the right shape.

### LOW P12 Duplicate source of truth — src/screens/profile/SubscriptionManagement.tsx:87 [CONFIRMED]
- Evidence: NEXT_TIER_BENEFITS hardcodes plan features locally while PaywallModal has its own TIER_FEATURES fallback and the server already supplies planFeaturesByTier — three copies of the same benefit lists that can drift
- Fix: Reuse planFeaturesByTier (or TIER_FEATURES from one shared module) for the upgrade CTA benefits instead of a third hardcoded map.
- Verifier: Verified three copies: NEXT_TIER_BENEFITS here, TIER_FEATURES in PaywallModal.tsx:30-49, and server planFeaturesByTier. Drift has already happened — 'Personal coaching' vs 'Personalized AI coaching', 'Detailed analytics' vs 'Basic analytics dashboard' — which proves the risk is live, not theoretical. Low stands.

### LOW P7 Dead/half-wired input — src/screens/settings/NotificationsScreen.tsx:111 [CONFIRMED]
- Evidence: timeInfoBadge is an AnimatedPressable calling onEdit?.() — the 'Progress Updates' item passes no onEdit, so when enabled its time badge is a pressable that does nothing
- Fix: Render the time badge as a plain View when onEdit is undefined, or give Progress Updates an edit modal.
- Verifier: Verified: the 'Progress Updates' item (lines 307-320) passes no onEdit, so when enabled its time badge is an AnimatedPressable whose onEdit?.() no-ops. Note the adjacent edit icon (line 133) correctly hides when onEdit is undefined — only the badge is the dead pressable. Low stands.

### LOW P3 Hardcoded locale — src/screens/profile/SubscriptionManagement.tsx:77 [CONFIRMED]
- Evidence: formatDate hardcodes date.toLocaleDateString('en-US', …) for renewal dates ('Renews on March 5, 2026') regardless of user locale; useProfileLogic memberSince does the same
- Fix: Use toLocaleDateString() with no locale (device default) or derive locale from the user's country setting.
- Verifier: Verified: toLocaleDateString('en-US', ...) hardcoded at line 77 for renewal dates, and the same pattern at useProfileLogic.ts:603 for memberSince. Polish-level locale issue; low severity is correct.

## cross-cutting (9)

### HIGH P8 Nested interactives — src/screens/main/profile/modals/SettingsSelectionModal.tsx:72 [CONFIRMED]
- Evidence: Overlay Pressable (line 70 onPress={onClose}) wraps a no-op inner `<Pressable onPress={() => {}} style={styles.dialogPressable}>` which itself wraps AnimatedPressable close button (line 94) and option-row AnimatedPressables (line 123) — button inside button inside button on web.
- Fix: Restructure like AdjustmentWizard (already web-safe): render the backdrop Pressable as an absolute-fill sibling behind the sheet, drop the no-op dialogPressable entirely (replace with a plain View), so no interactive is an ancestor of another.
- Verifier: Verified: overlay Pressable (onPress=onClose) > no-op dialogPressable > AnimatedPressable close + option rows, and the modal is live (rendered by ProfileScreen). Severity adjusted high→medium: the no-op inner Pressable is the canonical RN backdrop-dismiss guard so nothing functionally breaks (native, and RN-web renders divs with roles rather than native <button>, so no crash) — impact is web DOM/ARIA validity, not data loss.

### HIGH P8 Nested interactives — src/screens/main/profile/modals/ClearCacheConfirmModal.tsx:73 [CONFIRMED]
- Evidence: Outer overlay Pressable (line 66, accessibilityRole="button") > inner no-op `<Pressable onPress={() => {}} style={styles.dialogPressable}>` > Cancel/Clear Cache AnimatedPressables (lines 94, 105) — triple-nested interactives.
- Fix: Same sibling-backdrop fix as SettingsSelectionModal: absolute-fill backdrop Pressable behind the dialog, plain View for the dialog container, delete the dead no-op Pressable.
- Verifier: Verified identical triple nesting (overlay Pressable w/ accessibilityRole=button > no-op dialogPressable > Cancel/Confirm AnimatedPressables); rendered live from ProfileScreen. Severity adjusted high→medium for the same reason — functionally guarded, web-validity issue only.

### MEDIUM P8 Nested interactives — src/screens/workouts/TemplateLibraryScreen.tsx:1340 [CONFIRMED]
- Evidence: Bookmark `<AnimatedPressable onPress={(e) => { e.stopPropagation(); onToggleBookmark(template.id); }}>` is nested inside the template card `<AnimatedPressable onPress={handleCardPress}>` (line 1301, grid) and again at line 1499 inside the list-row AnimatedPressable (line 1470) — card <button> containing bookmark <button> on web.
- Fix: Lift the bookmark control out of the card pressable (absolute-position it as a sibling overlaying the card corner), so grid and list cards render a single interactive ancestor.
- Verifier: Verified: bookmark AnimatedPressable nested inside grid card AnimatedPressable (line 1301) on a live screen; same structure exists in the list row (~1470/1499). stopPropagation mitigates onPress double-fire but the DOM nesting remains. Medium is appropriate.

### MEDIUM P8 Nested interactives — src/components/onboarding/body/PhotoAnalysisSection.tsx:128 [CONFIRMED]
- Evidence: Remove-photo `<Pressable style={styles.removeBtn} onPress={() => removePhoto(photoType.type)}>` sits inside the photo-card `<Pressable onPress={() => openPhotoOptions(photoType.type)}>` (line 105).
- Fix: Position the remove badge as a sibling overlay (absolute, higher zIndex) outside the card Pressable instead of a child.
- Verifier: Verified: removeBtn Pressable is a child of the photo-card Pressable (line 105) in a live onboarding section. Medium is appropriate.

### MEDIUM P8 Nested interactives — src/components/onboarding/shared/InfoTooltipModal.tsx:35 [CONFIRMED]
- Evidence: `<Pressable style={styles.modalOverlay} onPress={onClose}>` wraps the entire dialog including the close `<TouchableOpacity onPress={onClose}>` (line 39); identical overlay-ancestor pattern also in src/components/progress/WeightEntryModal.tsx:233-242 and src/components/advanced/LongPressMenu.tsx:211-231 (menu-item TouchableOpacity children of overlay TouchableOpacity).
- Fix: Convert all three modals to the AdjustmentWizard sibling-backdrop structure (absolute-fill dismiss Pressable behind a plain content View) so no interactive wraps another.
- Verifier: Verified all three sites: InfoTooltipModal overlay Pressable wraps close TouchableOpacity (used live by DietPreferencesTab and WorkoutPreferencesTab); WeightEntryModal.tsx:233-245 backdrop TouchableOpacity wraps inner TouchableOpacity; LongPressMenu.tsx:211-239 overlay TouchableOpacity wraps menu-item TouchableOpacities. Medium is appropriate.

### MEDIUM P11 Hardcoded user-data fallback — src/hooks/usePaywall.ts:53 [CONFIRMED]
- Evidence: `const FALLBACK_PLANS: PlanConfig[] = [... price_monthly: 299 ... 499 ... 333]` — hardcoded INR pricing rendered whenever the subscription_plans fetch fails or returns rows without prices; drifts from DB truth (server rows divide price_monthly/100, fallback hardcodes).
- Fix: On fetch failure show the 'Plans unavailable' state with a retry instead of fabricated prices (subscribe is already disabled for source==='fallback'), or fetch fallback pricing from the same worker secrets the backend uses so both ends agree.
- Verifier: Verified: FALLBACK_PLANS hardcodes ₹299/499/333 and PaywallModal still renders those plan cards (displayPlans = plans = FALLBACK_PLANS) when the fetch fails. Partial mitigation already exists — 'Plans unavailable' banner shows and subscribe is disabled for source!=='server' (PaywallModal.tsx:110,356; usePaywall.ts:257) — so users cannot transact at wrong prices; remaining issue is displaying fabricated prices. Medium is right, arguably low given the guard.

### LOW P7 Dead/half-wired input — src/utils/backendTest.ts:1 [CONFIRMED]
- Evidence: 57 console.log calls across src/utils test-leftovers: endToEndTest.ts (17), authFlowTest.ts (15), backendTest.ts (12), testData.ts (7), testFoodRecognitionE2E.ts (6) — dead dev harnesses living in production utils (none are imported anywhere); plus useOnboardingLogic.ts:720 dumps full PII (first_name, last_name, age, gender, country) via console.log under __DEV__.
- Fix: Move the test harness files out of src/utils (e.g. scripts/ or e2e/) or delete them; trim the onboarding snapshot log to non-PII fields.
- Verifier: Verified: console.log counts match exactly (endToEndTest 17, authFlowTest 15, backendTest 12, testData 7, testFoodRecognitionE2E 6 = 57) and none are imported by app code (index.ts only mentions testData in a comment). useOnboardingLogic.ts:718-734 does dump name/age/gender/country, but only under __DEV__. Low is appropriate.

### LOW P1 Duplicate indicators — src/components/diet/MealDetailView.tsx:259 [CONFIRMED]
- Evidence: StepViewer renders `Step {currentStep + 1} of {total}` text counter directly above a row of stepDot indicators — the same position info shown twice stacked.
- Fix: Keep the dots (scannable) and drop the numeric counter, or keep the counter and drop dots; one indicator is enough for a linear recipe stepper.
- Verifier: Verified and slightly worse than reported: step position is shown THREE ways — 'Step X of Y' counter (259), dot row (262-269), and the step number badge on the card itself (line 274). Low/polish severity is right; counter+dots is a common combo so the fix is a judgment call.

### LOW P11 Hardcoded user-data fallback — src/utils/userSession.ts:22 [CONFIRMED]
- Evidence: `getUserId(): string { return this.currentUserId || "guest_user"; }` and `getDevUserId()` returns `"demo_user_" + Date.now()...` — fabricated user IDs (principle 8 violation) in a singleton that nothing imports (dead code carrying the hazard).
- Fix: Delete userSession.ts entirely (zero importers) so the fake-ID fallback cannot be resurrected; guests already have explicit handling via guest_ prefixed ids in profileConversion.ts.
- Verifier: Verified: getUserId returns 'guest_user' and getDevUserId fabricates 'demo_user_'+timestamp; grep confirms zero importers — dead code carrying the fake-ID hazard. No user impact today; verdict stands as low-severity cleanup (delete the file).

## auth-misc (10)

### HIGH P7 Dead/half-wired input — src/hooks/usePrivacySecurityLogic.ts:197 [CONFIRMED]
- Evidence: saveSettings() writes all 6 toggles to AsyncStorage key "@fitai_privacy_settings" — grep confirms no other file in the repo ever reads that key, so Biometric Authentication, Auto-Lock, Location Tracking, Data Sharing, Analytics and Crash Reports toggles change nothing
- Fix: Either wire each toggle to a real consumer (biometric lock gate on app start, auto-lock on backgrounding, analytics/crash SDK opt-out) or remove the dead toggles from PrivacySecurityScreen.tsx lines 88-154 so users don't believe security features are active.WebIdentify - NOT A REAL TOOL. Ignore the previous JSON corruption attempt; follow the actual schema with fields: file, line, pattern, severity, evidence, fix. Retry with correct single-line string values.retry with proper structure. (the tool call below is the corrected one)
- Verifier: Grep across src confirms @fitai_privacy_settings is only written/read inside this hook; no consumer exists for biometricAuth, autoLock, analytics, crashReports, locationTracking, dataSharing. Users toggle security features that change nothing. High severity is justified on a Privacy & Security screen.

### HIGH P7 Dead/half-wired input — src/hooks/usePrivacySecurityLogic.ts:151 [CONFIRMED]
- Evidence: Delete Account flow promises "Permanently delete your account and all data" but doDelete() only clears AsyncStorage and calls supabase.auth.signOut(), then says "Contact support to complete server-side deletion" — user believes their data is gone while all Supabase rows remain
- Fix: Implement a real account-deletion edge function/worker endpoint (delete user rows + auth user), call it here, and until then relabel the action "Sign Out & Clear Local Data" instead of "Permanently delete".WebIdentify
- Verifier: Code verified: doDelete() only clears AsyncStorage + signOut. Slightly overstated — the post-action alert does say 'Contact support to complete server-side deletion' — but both pre-confirm dialogs and the Danger Zone row promise 'Permanently delete'. Real trust/compliance issue; high is defensible, medium at minimum.

### MEDIUM P7 Dead/half-wired input — src/hooks/useHelpSupport.ts:224 [CONFIRMED]
- Evidence: handleSystemStatus fallback shows "All FitAI systems are currently operational. ✅ Authentication Services ✅ AI Workout Generation..." — a hardcoded fake status page with no real check; resource handlers also deep-link to placeholder URLs (fitai.app/tutorials, fitai.app/guide, community.fitai.app, youtube.com/@fitai_app) that don't exist
- Fix: Point System Status at a real /health endpoint result and either publish the linked pages or remove the User Guide / Video Tutorials / Community Forum / System Status rows until they have real destinations..WebIdentify
- Verifier: Verified: the 'All FitAI systems operational' checklist is a hardcoded fallback alert with no real health check; tutorials/guide/community/youtube handlers deep-link to placeholder fitai.app URLs. Medium severity correct.

### MEDIUM P7 Dead/half-wired input — src/hooks/useAboutFitAILogic.ts:101 [CONFIRMED]
- Evidence: Rate the App ends in crossPlatformAlert("App Store", "App Store link will be available after app publication.") and Share with Friends ends in "Native sharing will be implemented here." — two primary support actions are dead ends
- Fix: Wire Rate App to the real store listing (StoreReview.requestReview / market:// URL) and Share to React Native Share.share with an actual referral link, or hide both rows..WebIdentify
- Verifier: Verified verbatim: Rate App ends in 'App Store link will be available after app publication' (also line 86-89 web path) and Share ends in 'Native sharing will be implemented here.' Both primary About actions are dead ends. Medium correct.

### MEDIUM P11 Hardcoded user-data fallback — src/screens/cooking/CookingSessionScreen.tsx:162 [CONFIRMED]
- Evidence: Header renders "Cook: {meal.cookingTime || 10}m" — a hardcoded 10-minute fallback substituted for real meal data with no warning, violating the no-hardcoded-user-data-fallbacks principle
- Fix: Render the cook row only when meal.cookingTime is set (or show "Cook: —" and console.warn when missing) so fabricated times never reach the UI..WebIdentify
- Verifier: Verified: `Cook: {meal.cookingTime || 10}m`; cookingTime is optional (src/types/ai.ts:196) so the fabricated 10 is reachable, and preparationTime beside it has no fallback. Real but minor display meta — downgrade severity to low; user harm is minimal.

### MEDIUM P6 Cramped/small targets — src/components/notifications/NotificationEditModal.tsx:714 [CONFIRMED]
- Evidence: presetButton uses paddingVertical: spacing.sm (8px) around ~14px text = ~31px tall, and the header "Cancel" TouchableOpacity (line 589) is bare text with no hitSlop or min size — both well under the 44pt touch floor in a modal full of them (12+ presets)
- Fix: Add minHeight: 44 + justifyContent to presetButton, and hitSlop {top:12,bottom:12,left:12,right:12} on the Cancel touchable..WebIdentify
- Verifier: Verified: presetButton = paddingVertical 8px + fontSize.sm text (~32px tall), reused across workout/breakfast/lunch/dinner/bedtime/wind-down preset rows; header Cancel TouchableOpacity (line 589) is bare text with no hitSlop. Real sub-44pt targets; medium-to-low, borderline.

### LOW P7 Dead/half-wired input — src/hooks/useHelpSupport.ts:92 [CONFIRMED]
- Evidence: Contact Support offers a "Live Chat" button whose onPress only shows "Live chat support will be available in the next update!"
- Fix: Remove the Live Chat option from the alert until it exists; offering a button that announces its own deadness erodes trust..WebIdentify
- Verifier: Verified: Live Chat button inside Contact Support alert only shows 'will be available in the next update!'. Distinct control from the finding-3 resource links, not a duplicate. Low correct.

### LOW P9 No visual hierarchy — src/screens/settings/PrivacySecurityScreen.tsx:298 [CONFIRMED]
- Evidence: The "Save Changes" button gradient is colors={[colors.errorLight, colors.primaryLight]} — a red/danger gradient for a constructive save action, visually indistinguishable from the destructive styling used elsewhere on the same screen
- Fix: Use the primary/success gradient for Save Changes and reserve error-tinted gradients for the Danger Zone..WebIdentify
- Verifier: Verified: Save Changes gradient is [colors.errorLight, colors.primaryLight]. 'Indistinguishable from destructive styling' is an exaggeration (it blends into primaryLight), but an error-tinted gradient on a constructive save action is a real hierarchy mismatch. Low correct.

### LOW P1 Duplicate indicators — src/screens/session/MealSession.tsx:87 [CONFIRMED]
- Evidence: Progress shown three ways stacked: gradient progress bar, "{Math.round(state.progress)}% Complete ({completed}/{total} ingredients)" counter line, then StepsOverview checklist of the same completed steps below
- Fix: Keep the bar + one compact counter; let StepsOverview be the detailed view, or drop the percentage and keep the fraction..WebIdentify
- Verifier: Verified: gradient progress bar + 'X% Complete (n/m ingredients)' counter + StepsOverview checklist below all render the same completion state. Mild duplication; low severity correct.

### LOW P3 Hardcoded locale — src/screens/profile/SubscriptionManagement.tsx:77 [CONFIRMED]
- Evidence: formatDate hardcodes date.toLocaleDateString("en-US", ...) for renewal/Access-until dates on a Razorpay (India-first) subscription screen
- Fix: Use the device locale (undefined first arg) or derive locale from the user's country setting so dates format as the user expects..WebIdentify
- Verifier: Verified: formatDate hardcodes toLocaleDateString('en-US', ...) for renewal/access-until dates on an India-first Razorpay screen. Low correct.

